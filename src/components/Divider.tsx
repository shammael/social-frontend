import { colorTokens } from '@/themes';

const Divider = () => {
  return (
    <div
      className={`h-[0.2px] dark:bg-neutral-500 dark:bg-opacity-40 bg-slate-300 my-2`}
    ></div>
  );
};

export default Divider;
