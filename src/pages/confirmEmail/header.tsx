import Logo from '@/components/Logo';

const Header = () => {
  return (
    <header className="w-full bg-white px-4 py-2">
      <Logo className="font-bold text-3xl text-sky-400" />
    </header>
  );
};

export default Header;
